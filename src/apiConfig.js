const apiConfig = {
    baseURL: 'http://localhost:3001/api',  // Base URL for the API
    endpoints: {
        users: '/users',                   // Endpoint for user data
    }
};

export default apiConfig;
